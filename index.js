// Function Expression for divide
const divide = function() {
    return 2000 / 100;
  };
  
  // Arrow Function for square
  const square = x => x ** 2;
  
  // Arrow Function for add
  const add = (a, b) => a + b;